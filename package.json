{
  "name": "task-app",
  "version": "1.0.0",
  "description": "Project Uses Ganache to run a local blockchain to deploy the contract and Web3 from CDN.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Detektive",
  "license": "ISC",
  "devDependencies": {
    "ganache": "^7.9.2"
  }
}
